# 项目协作约定

## 前端设计

所有前端界面设计、重设计、视觉优化、组件样式、交互体验和设计系统相关任务，都必须使用 `impeccable` skill，并在修改设计前遵循其工作流。

## 自动提交与推送

用户已授权：每次完成本项目的修改后，自动提交并推送到 GitHub，无需再次询问。

- 目标仓库：`https://github.com/lyhlyhlyh233/migrationagentdemo.git`，当前分支为 `main`。
- 完成当次任务所需的检查后，检查差异，提交本次任务相关的文件并推送；遵循用户对验证范围的要求。
- 提交说明应概括实际改动，不要将无关的用户修改一并提交。
- 保持 `.gitignore` 的排除规则，不提交依赖目录、构建产物、本地缓存或凭据。
- 使用普通推送，不强制覆盖远端历史。遇到权限、网络或冲突问题时，说明推送尚未完成及具体原因。
- 完成后简要报告同步结果，可附提交编号。
