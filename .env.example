# All VITE_ variables are public. Never place credentials here.
VITE_SERVICE_MODE=mock
# Reserved for the future HTTP adapter; no endpoints are implemented yet.
VITE_API_BASE_URL=
# Use / for root hosting, /migration/ for a subdirectory.
VITE_DEPLOY_BASE=/
