# Product

## Register

product

## Users

迁移项目经理与实施工程师。日常工作包括准备资料、处理风险、确认迁移计划、监控任务与验收交付。

## Product Purpose

MigrationDirector Ultimate 通过中央对话组织四阶交付：调研评估、规划设计、迁移实施、结果验证。顶部用于进度和阶段切换，右侧提供共享步骤与统计。规划、任务、风险、交付件、日志提供单独管理页面。

首次进入空白工作空间，可新建项目或先开启临时对话。项目创建后自动进入评估主会话；后续每一阶段由用户人工确认进入，再创建该阶段主会话。阶段内可增加子会话，独立讨论并共享项目任务。

## Brand Personality

克制、清晰、专业。有辨识度的细节来自文字层次、石墨用户气泡和语义状态色，不依赖彩色智能体大卡、渐变或装饰动画。遵循 [DESIGN.md](DESIGN.md)。

## Current Capabilities

- 独立 Vite 前端，支持内网静态部署。
- 多项目、多会话的页面会话内隔离，后台结果写回原会话。
- 阶段引导、迁移调研表模板下载、资料上传入口、可选风险处置、自动排除受阻对象、人工交接、任务确认与验收。
- 右侧默认显示窄执行详情栏；打开风险后替换为可拖动、可折叠的风险面板，页签支持关闭并恢复执行详情。评估完成后自动展开风险一次。面板默认与对话按 38∶62 分配，数量入口定位独立管理页的相关虚拟机。批量策略在顶部、单项策略在对应风险下方。独立页按类别或虚拟机处理，支持分页子表、跨类别多选、批量采用建议、忽略或不迁；默认保留已有策略，主动覆盖时重新核验。
- 结构化回答：资料输入、报告解读与迁移建议、任务状态、交付文件、人工确认；不提供独立的总体方案选择表单。
- 全局白/黑/绿/红主题、背景和中英文；Agent 和模型切换。
- Nexent API Key / 账号密码配置与退出交互。
- 规划阶段接续评估范围，对话内提供导表、模板下载和默认折叠的八项基础约束表单，表格主要补充业务属性及依赖，也可一起填写带宽等条件。生成规划前右侧仅显示进度详情，生成后“规划设计”页签保留摘要、迁移约束和当前导入/调整；“迁移规划”打开完整工作台。规划、风险、任务和验证管理页共用最近阶段会话，右侧对话默认约 34%，窄屏切换列表/对话。
- 业务属性可按虚拟机或系统批量填写；导入先预览后应用，保留已有字段。默认生成完整覆盖当前范围的 8 批次，支持窗口/缓冲、批次归属调整预览、分页多选、资源占用、甘特图和五 Sheet XLS 导出。资料修改后显式重新生成，人工交接后只读。

- 实施按规划范围和稳定虚拟机 ID 建立任务。Migration 连接检测成功后，用户选择批次、确认预览，自动创建、全量并保持增量；割接需人工确认。未创建对象可移批次，已启动对象只能改后续窗口，保留批准的规划基线。
- 同步、暂停、恢复、主动增量和重试共用服务状态。网络、容量、权限异常可演示日志获取、诊断、方案确认、修复和复查，不自动绕过人工确认。
- 首批割接后人工进入验证，其他批次可继续实施。技术差异、业务验证和问题反馈分别记录；支持批次/业务系统/虚拟机选择、实际附件下载、阶段性报告，所有纳入对象通过才允许最终交付。

## Prototype Boundary

所有业务执行为 Mock，无真实模型、迁移或认证服务。上传接收文件但不解析 Excel 内容。规划按业务等级与风险排序，依赖、集群和约束提供可解释提示；日期和时长是模拟估算，没有真实排程求解。只支持明确列出的对话调整示例，其他要求先澄清。刷新重置业务状态，外观偏好保留。HTTP 模式明确提示未接入。

业务条件见 [业务逻辑说明.md](业务逻辑说明.md)，代码结构及适配方法见 [docs/design.md](docs/design.md)。
