# MigrationDirector Ultimate 视觉规范

面向迁移项目经理与实施工程师的日常工作台。沿用对话为主、进度为辅的布局，参考用户提供的 Codex 截图增强文字对比和业务输出层次。架构与适配说明见 [docs/design.md](docs/design.md)。

## 色彩与主题

默认白色，设置中依次提供白、黑、绿、红四种主题。青绿和酒红只用于导航选择、关键动作与少量模块标记，不给不同智能体分配彩色大卡。使用系统字体，不依赖外网字体。

- 正文：中性高对比色；辅助文字以 `--muted` 区分，不通过透明度降低整块内容可读性。
- 完成：`--success` 绿色；执行中：`--info` 蓝色。
- 待处理/提醒：`--warning` 琥珀色；失败/高风险：`--danger` 红色。
- 状态同时提供文字或图标，不能只靠颜色表达。
- 全局背景覆盖导航、对话和右侧详情，保留左右分隔线。移动导航和浮层采用不透明背景。

语义色、正文与气泡变量在 `src/styles/tokens.css`；原有主题、背景与基础控制变量在 `src/styles/index.css`。不要在功能文件里另加同义色系。偏好即时应用并在本地保存，白色/无背景/中文为默认值。

## 对话与业务结果

三个浅色主题的用户气泡为深石墨底、浅色字；深色主题使用与画布区分的灰色表面。助手内容开放排版，无大气泡。

助手正文使用 `--body-size`：常规和小屏 1rem，2K（CSS 视口宽度 1920px 起）1.125rem，行高约 1.6。界面文字使用固定 rem 层级：`--ui-size` 0.875rem、`--ui-small` 0.8125rem、`--ui-caption` 0.75rem，局部标题为 `--ui-title` 1rem。菜单、页签、表格和表单按相同职责使用同一字号，不在侧面板重复放大。常规控件高 2.25rem，紧凑控件 2rem，触屏按钮至少 44px。关键结论通过字重强调；消息不重复平台名或每轮时间，耗时以整秒展示。

输入框独立使用 `--input-size: 1rem`，阶段会话与临时会话一致，不随 2K 回答正文放大。提示与实际输入均为常规字重 400、1.5 倍行高；提示使用 `--muted`，输入使用 `--text`，提示对比度至少 4.5∶1。Agent、模型、快捷菜单及常规表单控件使用 0.875rem，紧凑表格操作使用 0.8125rem，辅助信息和耗时使用 0.75rem。使用同一系统字体栈；以浏览器最终计算样式核对，不在文件末尾不断叠加字号修正。

助手回答可包含可折叠的思考摘要、最终文本和业务结果块。摘要是模拟服务编写的说明，不是真实模型思维链。保留有效的复制正文操作，不增加没有动作的图标。

结果块采用紧凑标题、列表及底部必要操作，10–12px 圆角和细边框。以迁移摘要、任务、交付文件及人工确认呈现，不照搬代码 diff 或文件修改统计。不嵌套大卡片。默认三项，剩余内容可展开；详细管理继续使用已有页面。

评估完成后，资料准备回合默认折叠为历史记录。正文、统计和文件属于同一轮回答；最近一条实质回答保留一个复制入口，简短操作回执不显示思考计时。评估指标采用紧凑横排，交付件采用文件名与下载操作行，避免统计大卡片挤占结论阅读空间。

## 布局与导航

桌面左侧栏约 256px，2K 下 292px，直接按 100% 浏览器缩放设计。无风险面板时，执行详情作为独立窄栏显示，普通桌面约 280px、2K 下 320px。打开风险后替换详情栏，风险面板默认占剩余工作区 38%，对话区 62%；手动拖动可以重新分配。关闭面板后，对话阅读列随宽屏扩展，1920px 起上限 1560px，并受可用空间约束。进度和输入框与阅读列对齐；阶段表单约 640px，宽屏约 700px，保持对话为主。

对话区保留原生滚动能力，滚动时显示滚动条，停止约 800ms 后隐藏滑块；预留固定滚动条空间，显隐不引起正文宽度跳动。管理页面、抽屉和右侧详情仍使用常规滚动条。

侧栏顺序：项目管理五入口（规划、任务、风险、交付件、日志）→ 四阶交付会话 → 临时会话。文字和标题统一 14px，紧凑桌面行距，移动端提供较大点击区域。当前用户和设置按钮位于底部，品牌与项目选择器位于左上角。

四阶只在阶段会话与初始空白工作区显示。首页和评估启动前使用流程预览，不显示 0%。管理页不展示顶部流程或执行详情；规划、风险、任务和验证管理页共用阶段对话输入框。临时对话隐藏流程和详情。进入阶段须人工确认，确认前在对话中展示范围和影响。风险策略可跳过，受阻对象自动排除。

桌面侧栏可折叠；手机使用导航抽屉。较窄桌面右侧详情可收起。2K 下保持阅读宽度；风险表格按容器分配列宽，窄屏按字段重排，避免左右滚动。

## 组件与可访问性

思考期间，发送箭头原位切换为可点击的实心方块停止按钮，提供“停止回复”的悬停提示和无障碍名称。停止后恢复发送状态。Agent 旁不再重复放“＋”，阶段操作从输入框上方快捷菜单进入。

按钮、表单、状态、结果框架、空状态从 shared/ui 复用。Select 为自定义下拉，支持方向键、Home/End、Enter、Escape、Tab 和输入检索，并处理弹层边界与模态对话内的定位。

Agent 在输入框左下，模型在右下；分类快捷操作紧贴输入框上方。子会话初始仅提供短上下文，选择操作才展开表单。评估主会话自动发起首轮对话，资料输入嵌在回答中；长回答优先定位到文字起点，桌面两份资料并排压缩显示。“迁移调研表”标题旁提供图标加文字的模板下载按钮，与上传标签独立，下载不触发文件选择。

风险以原始发现、推荐策略和是否纳入工具范围区分，不用勾选代表技术问题已经解决。评估答复不展示总体方案摘要和选择表单；在交付文件下方、复制按钮上方左对齐呈现深色“查看&处理风险”和次要“继续下一阶段：规划设计”两个快捷入口，统计区不重复放风险按钮。报告答疑收在输入框上方的快捷菜单中。输入框上方右侧保留深色阶段按钮，文案统一为“继续下一阶段：阶段名称”，与答复内的规划快捷入口共用人工交接规则；已进入的阶段显示“打开阶段名称”。

执行详情不属于右侧面板页签，在没有展开工作面板页签时显示。风险使用非模态面板，无遮罩、不锁定对话，不重复显示项目名称。“迁移风险”以紧凑页签呈现，标题旁带关闭叉，支持 Delete 关闭；右上角保留折叠图标，其左侧使用带边框的“打开迁移风险页面 ↗”按钮。关闭或折叠风险后恢复窄执行详情栏，可从对话工具栏重新展开风险，筛选、多选、分页和未提交内容保持。桌面分隔线支持拖动及左右方向键调整，面板至少 420px，对话至少保留 360px；宽度在当前页面会话中保留。1180px 及以下风险面板独占内容区并隐藏拖动条；关闭后恢复执行详情。980px 及以下详情也独占内容区，收起详情后返回对话。评估完成后自动切到风险页签一次，不抢走输入焦点。

面板专注类别处理，采用带复选框的类别目录和当前类别表格；名称切换类别，复选框只改变选择。大类始终保留左侧纵向目录，随容器缩窄减少目录宽度；右侧展示小类。同规则同影响的发现合并展示。面板只显示可点击的虚拟机数量，点击跳到独立页并限定相关虚拟机，范围可清除。独立页保留有表头和分页的紧凑子表，证据按需展开。独立页通过“按类别／按虚拟机”切换，支持单台例外和整改核验。

风险主表按多选、风险事项、级别、迁移影响、虚拟机数、当前策略、操作分列；合并事项显示最高级别，规则依据和评估建议通过“查看详情”展开。操作列使用带图标和边框的紧凑按钮：“查看详情”为中性描边，“设置策略”为主题浅底；侧面板内等宽纵排，宽管理表格并排，窄屏重排后回到横向按钮组。虚拟机数量用数字和单位的紧凑按钮，独立页子表展开入口另带箭头，数字为 1rem、等宽数字。主表采用灰色表头，虚拟机子表采用白色表头与轻微底色的内容行；深色模式对应使用主题表面色，避免刺眼白块。子表按名称、标识、迁移资格、本项策略、操作分列，一台一行；独立虚拟机列表另列筛选范围内的风险数和处理进度。级别、数量和操作列使用紧凑固定宽度，其余列按比例分配；长名称、影响和策略允许列内换行。表格自身容器不足 460px 时按字段标签重排，不裁掉内容或依赖左右滚动。分页总数不超过 10 项时仅显示“共 N 项”。

风险工作区顶部第一行放统计、视图切换及级别/状态筛选；下一行左侧为深色“批量设置策略”及推荐、忽略、不迁快捷操作，搜索靠右，选择范围和清空选择紧随其下。手机宽度才将搜索换行。顶部编辑区仅用于批量操作；单项策略在对应风险事项或虚拟机风险下方展开。一次只编辑一处，单项期间锁定类别、筛选、页码及多选，避免表单被切走。编辑复用同一组可见单选、迁移方式与说明输入，不新增弹窗。批量操作区含工具栏最高约工作区的 55%；编辑内容可滚动，取消和应用按钮始终可见。“覆盖已有策略”放在取消和应用按钮左侧，默认不勾选。桌面策略选项横向换行，通用说明收进“查看策略说明”，悬停选项也可查看提示；窄屏选项纵向排列。处理/保留/覆盖数量、重置验证警告与快捷操作影响仍直接显示。

编辑期间固定目标风险 ID，禁用筛选、模式切换和多选变化，仍允许浏览类别、翻页和展开依据。独立管理入口位于页签栏右侧，使用带边框的“打开迁移风险页面”按钮，编辑时禁用以保留草稿。编辑区无模态焦点锁，打开聚焦标题；关闭返回发起按钮，按钮不可见或已失效时返回策略操作区。失败原位保留输入，取消保留选择，成功清空选择。状态色用于级别与迁移影响，正文保持中性，不增加装饰卡片。

四阶选中背景包含完整按钮区域，内边距为纵向 8px、横向 12px；进度线与按钮间保留 5px，不扩大为卡片。
2K 下四阶随对话列加宽，按钮高至少 48px、内边距 12px/16px，进度线间隔 8px，阶段之间以箭头连接。流程预览按内容块宽度布局，箭头在相邻内容块之间的剩余空间居中，不按拉伸后的列边界定位；上下对齐内容块中线。较小屏幕保持紧凑布局。

设置使用右侧 560px 模态抽屉，包含通用与账户认证两页。背景、语言和主题自动保存；Nexent 配置需要手动保存，API Key 与账号密码分别说明输入规则，不提供平台地址字段。退出需要确认。

所有操作保留可见焦点。思考摘要及耗时与正式答复之间使用主题分割线，短操作回执不额外添加分割线。异步回复和通知使用适当 live region，背景任务不抢走会话。无渐变、装饰大卡或繁杂动画；遵循 prefers-reduced-motion，业务进度更新不依赖动画表达。

## 规划工作台

阶段会话继续采用对话 62% / 面板 38%，规划与风险共用页签栏和拖动分隔线。首次进入规划自动打开“规划设计”；没有展开工作区面板时恢复窄执行详情。左侧“迁移规划”位于迁移任务之前。

规划、风险、任务和验证管理页的对话占可用宽度约 34%，限制在 400–720px，可折叠。1280px 以下切换工作台/对话。管理页占满除导航外的空间，不继承正文阅读列最大宽度。聊天复用阶段记录、草稿及模型设置；阶段与会话标题相同时只显示一次。输入始终 16px，表单/页签 14px，紧凑操作 13px、辅助信息 12px。

规划侧面板为“规划摘要＋当前操作”：范围、批次数、预计周期、待核对项及管理页入口，约束表单、导入与预览一次显示一项。大表和图表只在独立规划页呈现。独立页四个视图为规划资料、批次计划、资源需求、迁移时间线。统计用一行数字，约束分带宽、窗口、日期与并发；业务属性按虚拟机或系统选择，依赖使用可编辑列表。容量位于资源视图，用占用条和数值呈现。

批次主表仅展示阶段、数量、资源、割接等核心字段，虚拟机标识和其他属性按需展开；完整字段见导出。表格允许列内换行，分页 20/50/100，区分本页和全部筛选结果。调整先展示修改前后、受影响范围，确认后应用；失败保留输入。待更新和模拟估算均有文字标识。

甘特图以蓝色表示同步、琥珀色表示割接、绿色表示验证和缓冲，附图例和日期；点击批次查看明细。对话保留结论、限制、紧凑统计及操作，不嵌入完整表格。上传明确说明实际表格尚未解析，不增加图表库、装饰卡片或动画。

## 实施与验证工作区

“迁移实施”“结果验证”沿用可关闭页签及 62∶38 比例，不新增独立抽屉。中间对话呈现可折叠摘要、解释、结果及操作入口；业务执行不受停止 AI 回复影响。实施面板按连接、批次、问题三项切换；连接字段带明确标签，模拟失败可重复演示。确认预览与错误留在原处，不弹逐台确认窗。

状态使用语义色和文字；批次进度条分同步、异常和已割接，统计分别显示割接与业务通过。同步趋势位于独立任务页，使用轻量折线和 MB/s 单位。任务表格保留分页，窄容器按字段重排；明细按需展开，不缩小字号塞满所有字段。

诊断方案用可见单选列表区分“可自动执行／需人工处理”，说明影响和前置条件；原位展示日志与附件。结果验证将技术差异、人工接受与业务结论分开，批量确认固定所选对象，变更冲突要求核对最新结果。最终交付按钮仅在全范围完成后可用。

管理页标题标明复用的阶段会话，当前批次或虚拟机以可清除的主题浅底标签呈现。查阅、筛选和翻页不发送消息；提交操作才写简短回执。主题颜色只点缀状态、选中页签、图表和诊断说明，正文保持中性。

## 品牌资产

平台名为 MigrationDirector Ultimate。导航与创建页共用 `shared/ui/BrandName`，Ultimate 使用独立品牌红色变量 `--brand-edition`，不随青绿或酒红主题改变色相；深色模式提高亮度以保持对比。网页标题使用完整名称。导航、创建页和网页 favicon 使用本地 Huawei 标志，保持原始红色和比例。背景来源见 [public/backgrounds/README.md](public/backgrounds/README.md)。

新建项目当前版本预填“华东数据中心迁移”和“上海代表处”，作为可编辑的表单初值；提交时仍使用用户修改后的值。
