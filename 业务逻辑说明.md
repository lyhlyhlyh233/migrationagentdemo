# MigrationDirector Ultimate 业务逻辑

本文件说明现有前端的业务行为；模块职责见 [项目架构](docs/design.md)，真实后端接入见 [接口适配说明](docs/integration.md)。当前所有执行均由 Mock 服务模拟。

## 项目与会话

工作空间初始无项目。新建项目填写名称、行业、迁移类型、地区部和代表处，随后自动进入调研评估主会话。未选择项目时，规划、任务、风险、交付件和日志不可操作，悬停或聚焦提示创建或选择项目。

项目资料、风险、任务、交付文件和进度由项目共享；每个阶段拥有独立主会话，也可新建子会话。子会话首次提问自动命名，并可重命名。消息、输入草稿、模型、Agent、等待状态与表单展开按项目及会话保存。

临时对话可在空白空间或项目中创建，独立保存记录。管理页与临时对话隐藏顶部四阶；左侧阶段历史仍可返回工作区。

## 四阶流程

| 阶段     | 主要输入/操作                           | 结果                             | 进入下一阶段的条件                           |
| -------- | --------------------------------------- | -------------------------------- | -------------------------------------------- |
| 调研评估 | RVTools 采集表、迁移调研表；启动评估    | 资产范围、评估风险、报告         | 评估完成，人工确认范围与交接；风险处置可跳过 |
| 规划设计 | 核对范围，填写业务信息/依赖/迁移窗口    | 批次计划、资源、时间线、RunBook      | 规划完成且无待更新/预览，人工交接；受阻对象不实施 |
| 迁移实施 | 检测连接，逐批启动、同步及人工割接       | 共享任务进度、割接结果、配置对比 | 至少有割接结果，人工确认进入验证             |
| 结果验证 | 源端/目标端配置对比，单台或批量人工验收 | 已确认虚拟机与批次完成状态       | 分批确认；全范围通过后确认最终交付         |

阶段条件统一位于 `src/domain/policies.ts`。顶部可以返回已进入阶段，并恢复最近会话。新阶段准备就绪只提示，不自动跳转；确认交接才新增下一阶段会话。

## 评估与规划

项目创建后自动发送“开始虚拟化迁移项目的调研评估”，助手显示思考摘要和引导文字，资料输入随该条回答出现。上传、采用示例、启动评估和保存风险策略会记录对话。旧资料输入收为历史记录，仅最新输入可操作。评估完成后准备过程统一折叠，最终结论、紧凑统计和下载入口呈现在同一条回答内；最新完整回答底部只保留一个复制入口。

评估完成后，当前项目的评估会话自动展开右侧“迁移风险”页签一次；若当时在其他项目、管理页或临时会话，返回该项目评估会话时再展开。此动作只改变查看位置，不自动选择策略、不重复生成对话，也不推进下一阶段。执行详情在未打开风险面板时以窄栏显示；关闭风险页签或折叠面板后恢复执行详情，风险编辑内容保留，并可通过对话工具栏重新展开。

Mock 初始范围为 200 台：180 台无风险，20 台风险虚拟机共涉及 26 条发现，归为 7 个大类、11 个风险事项。风险虚拟机中 6 台仅有可接受约束、10 台需调整或验证、4 台当前方案不支持；初始可纳入 186 台、排除 14 台。相同事项可影响多台，同一台可存在多项风险，统计按虚拟机去重。规则与报告结构参考用户提供的评估清单及 Excel/PPT 模板，数值为前端示例，不使用模板中的示例总数作为当前项目结果。上传调整后的范围模拟从实际清单减少 4 台，首次为 196 台；规划的 8 个批次完整分配实际可迁范围，不按固定 16 台截断；规划模板提供虚拟机清单、业务依赖、迁移约束三个工作表。

规划生成 8 个示例批次，检查提示从当前资料计算，不再注入固定的 4 条规划风险。详见下文“规划资料与结果”。

风险按平台兼容性、磁盘、规格、应用、容量、硬件、功能分类。右侧非模态面板按类别处理，数量入口跳到独立迁移风险页的虚拟机视图，限定该事项涉及的虚拟机；页面显示可清除的定位范围，一台时自动展开、多台分页。独立页面可切换类别和虚拟机视图，共用项目风险状态、策略编辑和证据呈现。类别中相同规则且相同影响的记录合并展示，无规则标识的记录保持独立。

类别与规则的批量操作默认保留已有选择和核验结果，可主动覆盖；执行时再次检查最新状态，防止默认模式下跨会话覆盖。可采用各条风险各自建议，也可选择适用的统一策略。单台页面支持逐风险和批量主动调整。无待处理项不要求再次确认，不以风险处理数量作为阶段门槛。

可选策略为接受约束（忽略）、整改后迁移、本次不迁、自定义策略；自定义可选择免代理、有代理或另行迁移/重建。接受约束仅适用于有约束但可迁的对象。整改计划与完成整改分开，后者需要人工填写验证依据。界面不再提供总体方案选择，迁移建议保留在答复正文中。评估答复底部提供风险处理和继续规划两个快捷入口，继续规划仍需人工确认，已开启时直接返回规划会话。

**不要求用户逐项确认风险。** 完成评估即可查看交接并继续规划。不兼容、未验证的整改项、显式不迁及另行重建对象按虚拟机去重后排除；同一虚拟机存在任一阻塞发现就不能纳入工具范围。约束类发现保留在风险和 RunBook 中。范围下载、规划批次与实施准备使用同一范围计算；MD 准备时按最新风险再过滤，受阻对象不生成工具任务。

风险目录的大类、小类和独立页虚拟机支持多选，批量范围按风险 ID 去重，跨页保留。所有“全部”操作只针对当前筛选结果的所有页；筛选变化会清空选择。默认只处理未选策略项，可在确认区主动勾选覆盖。已验证记录被覆盖后，原验证状态清除，必须按新方案重新核验。

“全部忽略”将约束项设为接受约束，需整改或当前不支持项设为本次不迁；“采用评估建议”逐项采用不同建议，“全部不迁”统一排除。提交前展示更新、保留、覆盖数量和预计项目排除范围；服务基于最新项目状态整体校验、一次写入，失败不留下部分策略修改。保存成功向发起会话写入一次简洁结果，失败保留界面选择与输入。虚拟机子表和独立列表的迁移资格始终按该虚拟机全部风险计算。

批量策略在统计与筛选下方、列表上方进行；单项编辑在对应事项或虚拟机风险下方展开，同一工作区只保留一处编辑。右侧风险面板与对话并排显示，打开后仍可输入和发送消息，不再用模态抽屉。打开编辑时固定风险 ID，期间不能更改筛选、模式或选择范围，批量编辑可继续浏览类别和翻页，单项编辑则固定类别和页码，仍可查看依据。单项入口只处理该事项，单台入口只处理该虚拟机范围；快捷操作仍遵守当前选择/全部筛选结果的范围。取消保留选择，失败保留输入和列表位置，成功清空选择。主表分列展示最高级别与迁移影响，子表不重复风险数量；独立虚拟机视图的风险数和进度按筛选结果统计，资格始终按项目全部风险计算。每张子表独立保存页码，总数不超过 10 项时只显示总数。

策略在 MD 准备开始前可调整；准备开始后锁定，防止已准备任务与新策略不一致。仍可补充人工验证依据，但不会自动向已生成的批次追加对象。风险原始发现及历史报告保留，不将跳过风险显示为闭环。

每个阶段的输入框上方右侧提供统一的继续入口，先在对话中说明范围和条件，再确认进入新阶段；最终验证阶段显示终点说明。

## 规划资料与结果

人工确认进入规划后，自动发送“开始迁移项目的规划设计”，回复思考摘要、评估范围及资料引导，提供补充资料、模板下载和生成初稿入口。继承最新评估策略，不要求再确认范围或先上传文件。右侧资料/批次/资源/时间线面板与独立工作台共享数据；规划和风险管理页的小对话复用最近阶段会话。

业务属性包括业务系统名称、业务等级、集群类型、集群角色，支持按虚拟机或业务系统多选批量填写。未修改字段保持原值。业务依赖包含上游（被依赖方）、下游（依赖方）、强弱程度及说明，可增删改；未填写不等于没有依赖。

约束固定为全量同步带宽(Gbps)、增量同步带宽(Gbps)、单批次最大停机窗口(h)、允许割接时间段、封网期、项目开始日期、业务验证时长(h)、单批次最大并发迁移数。带宽和时长须为正数，并发为 1–10000 整数。目标 vCPU、内存/存储 GB 和预留比例在资源视图调整，预留范围 0–99%。

上传只演示接收、示例解析预览及确认应用，不读取实际 Excel。示例导入填补缺失的系统和等级，保留已有字段，依赖列表为空时添加样例。范围上传兼容路径仍模拟减少四台。

### 生成与调整规则

- 从当前可纳入范围取资产，按业务等级、风险总分排序，稳定分配到 8 个示例批次，不重复、不遗漏；默认 200 台中纳入 186 台。先呈现试点，再核心业务和规模迁移，不代表识别了真实生产拓扑。
- 检查未知/循环依赖、强依赖跨批次、弱依赖顺序、集群角色、低带宽、停机窗口、并发分组、封网期和容量。强依赖只给出共同割接提示，**没有自动求解合并，日期和时长仍为模拟估算**。
- 保存资料或改变风险资格使旧计划待更新；显式重新生成才替换原批次。窗口/缓冲和 VM 归属调整先预览，再确认应用；取消保留原计划，失败保留输入。
- 只保留一份当前计划及一份预览，应用须匹配规划版本和发起会话。资料编辑/批次多选分别保留，分页默认 20，可切 50/100，跨页多选；筛选、展开、分页不写入对话。
- 对话支持“将 B02 割接改到周六”“降低单批次并发”和安排依据说明。降低并发示例为当前值减半，修改均先预览。其他自由要求先澄清，不自动执行。
- 人工交接要求计划已生成、无待更新范围和待确认预览；风险无需逐条处理。交接后只读，不支持实施中重排。

### 模板、导出和口径

输入 `.xls` 模板三 Sheet：虚拟机评估总览（含标识、资源、风险与策略，末尾四个业务属性）、业务依赖关系、迁移约束条件。“业务登记”和“全量同步贷款”分别统一为“业务等级”和“全量同步带宽”。

输出五个 Sheet，字段顺序由 `services/mock/planning-files.ts` 定义：

| Sheet | 内容 |
| --- | --- |
| 批次规划 | 售后评估状态、批次总数、预计总迁移周期、预计总停机时长、售前可迁移 VM 数、售后输入批次 VM 数、排除 VM 数 |
| 批次计划 | 批次编号/阶段、VM 数、vCPU/内存/磁盘总计、高中低风险数、停机估算、割接窗口、同步开始、割接时间、缓冲天数、状态 |
| 批次详情 | 批次、VM 名称、UUID、系统/等级/集群类型、GuestOS、vCPU、内存、磁盘数/容量、风险总分/等级、迁移方式/策略 |
| 资源需求 | 目标、预留、可用、需求及剩余；分批次新增/累计 vCPU、内存、存储和利用率 |
| 迁移时间线 | 日期列与同步/割接/验证色块；长周期最多 61 个日期列，并注明间隔 |

售前可迁移数是进入规划时的评估基线；售后输入数是批次去重资产数；排除数是评估总量减当前批次数量。周期从最早同步至最后验证/缓冲结束，停机为各批次估算合计；售后评估状态是模拟检查，不代表真实验证。界面、导出共用规划快照，历史对话统计保留当时结论。时间为无时区示例值，不代表目标机房时区。

## 实施与验收

### 连接、批次与调整

人工交接到实施后，自动发送开场对话并打开 Migration 连接表单。填写 IPv4、1–65535 端口、用户名、密码，检测成功才允许控制任务。修改草稿不影响有效配置；新配置检测失败保留原配置，但权限故障下失败检测不会恢复可用状态。正在执行控制操作时禁止替换有效连接。密码仅存在请求及本次表单内存，检测成功清空输入，不进入快照、日志、导出或浏览器持久存储。

规划确认时，按最新纳入范围初始化每台 VM 的执行状态：待创建 → 创建中 → 全量同步 → 增量同步 → 持续增量就绪 → 割接中 → 待业务验证。批次只负责分组，不再将不同批次作为创建、同步、割接专用批次。用户选择固定范围、核对目标资源池和网络映射后确认启动；按已批准并发限制推进。同步中的对象支持暂停/恢复，持续增量就绪对象可手动再增量一轮。

割接预览说明数量、窗口、同步及阻塞情况，提交再次检查。当前窗口和同步时间属于模拟信息，不校验现实封网时段；实际执行需后端实现窗口门禁。割接完成只生成技术核对记录，不自动通过业务验证。相同任务不能重复启动或割接。

仅待创建且未启动的对象可跨批次移动；已启动对象只能调整尚未执行的后续窗口。变化写入实施安排，批准的 `planning.batches` 不被改写。调整预览绑定对象、发起会话与实施 revision；取消不改变原安排，失效预览要求重建。项目中的其他会话共享同一任务与执行锁。

### 故障诊断

正常、同步网络异常、目标容量不足、权限不足四种模拟场景可重复选择。异常场景只影响本次启动的首台，其他对象按各自状态推进；权限故障使连接不可用并停止新的控制工作。

同一未解决故障只建立一条记录，自动获取带 MOCK 标识的日志并诊断。日志失败或诊断无结论可原位重试。网络方案可以经用户确认后模拟自动修复；容量和权限需人工处理、填写说明，可附证据，再确认复查。修复失败保留输入，不自动循环；复查成功后任务仍停在原处，必须主动重试。权限问题还必须先重新检测连接。

异步诊断和任务里程碑写回操作发起会话，不切走用户当前页面。任务按批次汇总消息，避免每台刷屏。对话中的明确操作只生成待确认预览；错误原因、方案解释、进度与交付件查询只答复，不执行。未覆盖的自由输入会澄清。

### 分批验证与反馈

首批割接结果产生后，人工确认进入已有四阶中的验证阶段；之后新结果持续加入，不重复创建会话。返回实施只是切换已有实施会话并定位相关批次，不撤销阶段、清除通过记录或重跑任务。

验证先对照批准资料的 CPU、内存、存储和 GuestOS，并呈现源网络、规划映射与模拟实际网络。示例按固定分布生成网络差异。预期差异填写至少 4 字的接受原因后可继续业务确认；意外差异通过问题反馈、处理说明和明确复查解决。差异接受不是业务验证通过。

业务状态为待验证/通过/不通过，支持按批次、业务系统筛选后跨页选择，最终逐台记录确认人、时间和说明。未填系统的对象按批次归组。批量通过前整体检查技术结果和未解决反馈；不能部分成功或修改未选对象。打开表单记录实施 revision，其他会话更新后要求重新核对，保留当前输入。

反馈可关联所选 VM、批次或本次全部迁移，附件保留实际文件字节并可下载。影响业务的反馈使相关记录不通过，普通建议不阻塞。处理说明提交后还须确认模拟复查，通过后恢复待业务验证，仍须人工确认业务通过。其他无关联批次可继续。未解决的明确强依赖阻塞会限制下游割接。

验证汇总包含批次/VM、配置对照、业务确认及反馈和诊断。未完成全部范围时标为阶段性结果；只有全部纳入对象均割接、技术条件满足、业务通过，才能二次确认最终交付，此后实施和验证只读。

## 结果、文件和日志

对话回答可包含纯文字、思考摘要、整秒耗时和业务结果块。历史摘要保存当次统计；任务和确认项通过 ID 读取当前状态，旧消息不能再次启动已执行操作。列表默认三项，其余可展开。

文件上传向服务传递 File；Mock 不解析内容。下载由服务返回文件 Blob、文件名和媒体类型，不由页面拼接业务文件。评估报告、计划和模板归档在交付件列表。范围清单、规划信息模板和风险处置方案随风险变化更新为当前内容；历史回答的统计快照不重算，文件引用下载当前版本。本轮没有历史文件版本库。任务日志下载从服务读取当前任务状态。

操作日志记录项目内操作及来源阶段、会话名称；右侧最近活动只展示当前会话的操作记录。普通消息不逐轮显示时间。

## 失败、切换与退出

请求失败显示错误并保留聊天输入，沿同一 requestId 重试可避免重复用户消息。取消或失败释放对应操作锁；切换项目/会话不取消任务、不改变结果归属。

思考期间可点击输入框的停止按钮，终止当前会话的回复或尚未完成的评估、规划生成及阶段引导，保留已上传资料及历史消息，恢复可再次执行的状态。会话显示“已停止回复”，不弹失败提示。停止按本次 runId 匹配，迟到或重复点击不影响新回复，也不停止已在后台执行的迁移任务和其他会话。

退出清空本次业务状态、文件、凭据、等待任务和订阅。重新进入为空白工作空间。刷新也会重置业务数据；外观、语言及背景偏好继续在浏览器保留。

## 真实接入前的边界

模型名 GLM 5.1、DeepSeek v4 为占位；思考摘要及回复是模拟文案。Nexent 配置仅保存、未验证。连接检测、任务控制、日志采集、诊断与复查只更新模拟状态；不连接真实迁移平台。真实后端还需完成认证、权限、持久化、文件解析、执行及事件协议适配。
